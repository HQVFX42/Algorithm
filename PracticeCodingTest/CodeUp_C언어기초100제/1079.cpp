#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	while (true)
	{
		char c;
		cin >> c;
		if (c == 'q')
		{
			cout << c << '\n';
			break;
		}
		else
		{
			cout << c << '\n';
		}
	}
}